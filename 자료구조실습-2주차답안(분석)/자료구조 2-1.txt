#include <stdio.h>
#include <stdlib.h>

int modulo(int a, int b) {
	if (a < b)
		return a;
	else
		modulo(a - b, b);
}
int main() {
	int a, b;
	scanf("%d %d", &a, &b);
	printf("%d\n", modulo(a, b));
}