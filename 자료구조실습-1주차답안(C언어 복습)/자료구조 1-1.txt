#include <stdio.h>

int sum(int n) {
	if (n) return n + sum(n - 1);
	return n;
}
int main() {
	int n;
	int total = 0;
	scanf("%d", &n);

	for (int i = 0; i < n; i++)
		total += sum(i + 1);

	printf("%d", total);
	return 0;
}
